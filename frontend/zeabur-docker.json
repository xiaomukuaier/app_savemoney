{
  "type": "backend",
  "build": {
    "type": "dockerfile",
    "dockerfile": "Dockerfile.static"
  },
  "env": {
    "VITE_API_BASE_URL": "${BACKEND_URL}"
  },
  "ports": [
    {
      "port": 80,
      "protocol": "http"
    }
  ]
}