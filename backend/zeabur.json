{
  "build": {
    "command": "pip install -r requirements.txt",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000"
  },
  "ports": [
    {
      "port": 8000,
      "protocol": "http"
    }
  ]
}